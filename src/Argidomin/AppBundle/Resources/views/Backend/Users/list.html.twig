{% extends 'AppBundle:Backend:index.html.twig' %}

{% block cuerpo %}

    <h2>Newsletter</h2>
    <div class="table-responsive">
        <table class="table table-bordered table-hover">
            <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Email</th>
                <th>Date</th>
                <th>Last modification</th>
                <th>Estatus</th>
            </tr>
            </thead>
            <tbody>
            {% for elemento in list %}

                <tr>
                    <td>{{ loop.index }}</td>
                    <td><a href="{{ path('editBackend',{'entidad':entidad,'objeto':elemento.email}) }}">{{ elemento }}</a></td>
                    <td>{{ elemento.email }}</td>
                    <td>{{ elemento.fechaCreacion | date ('d/m/y') }}</td>
                    <td>{{ elemento.fechaModificacion | date ('d/m/y') }}</td>
                    <td>{% if elemento.enabled == true %}Active{% else %}Disable{% endif %}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}